[package]
name = "dam-page"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
publish = false

[dependencies]
premise = "0.1.5"
dam-core = { path = "../dam-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
