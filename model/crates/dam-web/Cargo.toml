[package]
name = "dam-web"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
dam-page = { path = "../dam-page" }
spec = { path = "../spec" }
wasm-bindgen = "0.2"
